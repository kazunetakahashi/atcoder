#include <iostream>
using namespace std;

int T[10][10];
int N, K;

bool istherebug (int n, int x) {
  for (auto k=0; k<K; k++) {
    if (istherebug(n+1, ))
  }
}

int main () {
  cin >> N >> K;
  for (auto i=0; i<N; i++) {
    for (auto j=0; j<K; j++) {
      cin >> T[i][j];
    }
  }
}
